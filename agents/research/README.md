This is the Research Agent workspace.

## My Role
- Discover new metrics for the MM-Bracket-Flow simulator
- Write specs to `../../docs/spec/`
- Update `status.txt` every 5 minutes

## Current Task Queue
See `../../docs/agent_sync.json` for assigned tasks.

## Output Format
Specs go in `../../docs/spec/` as structured JSON files.
